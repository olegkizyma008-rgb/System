# MikroTik WiFi & MAC Address Spoofing Module

## Огляд

Цей модуль автоматично змінює конфігурацію гостьової WiFi мережі на MikroTik модемі та локальну MAC адресу на macOS при кожному запуску для захисту приватності та запобіганню відстеженню за IP/MAC адресою.

## Можливості

- 🔄 **Випадкова назва WiFi (SSID)** - генерується нова унікальна SSID для гостьової мережи
- 🌐 **Випадковий приватний підмережа** - IP адреси змінюються автоматично (10.x.y.0/24)
- 🔐 **Змінна MAC адреса** - локальна MAC адреса macOS змінюється на локально керовану адресу
- 🔒 **Захищений пароль** - пароль гостьової мережи залишається `00000000` для зручності
- 📊 **Статус моніторинг** - можна перевірити поточний стан WiFi та MAC адреси

## Вимоги

- MikroTik маршрутизатор з RouterOS 6.0+
- SSH доступ до мікротік (SSH ключ налаштований)
- macOS з правами `sudo` для зміни MAC адреси
- Python 3.7+ (опційно, для повнофункціональної версії)

## Установка

### 1. Встановити SSH ключ на MikroTik

```bash
# На вашому мак, скопіюйте публічний ключ:
cat ~/.ssh/id_ed25519.pub

# На MikroTik (через telnet/серійний з доступом):
/user ssh-keys add public-key-file=stdin user=admin
# Вставте публічний ключ та натисніть Ctrl+D
```

### 2. Перевірити підключення

```bash
ssh -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=no admin@192.168.88.1 "system identity print"
```

Має вивести інформацію про MikroTik.

## Використання

### Виконати спойфинг (зміна WiFi SSID та IP)

```bash
sudo /path/to/cleanup_scripts/mikrotik_wifi_spoof.sh spoof
```

Це здійснить:
- Генерацію нової SSID для гостьової мережі
- Генерацію нового приватного підмережа (10.x.y.0/24)
- Оновлення обох гостьових WiFi інтерфейсів (wifi3, wifi4)
- Спробу зміни локальної MAC адреси

### Виконати спойфинг із автоматичним перепідключенням ✨ (НОВИЙ)

```bash
sudo /path/to/cleanup_scripts/mikrotik_wifi_spoof.sh spoof-auto
```

Це здійснить все з команди вище, **плюс**:
- Автоматичне відключення від поточної мережи
- Автоматичне підключення до нової гостьової мережи
- Менш ніж 1 хвилина для повного перепідключення

### Перепідключитися до існуючої мережи

```bash
/path/to/cleanup_scripts/mikrotik_wifi_spoof.sh reconnect "Guest_ABC123"
```

Або з пароллю по замовчанню:

```bash
/path/to/cleanup_scripts/mikrotik_wifi_spoof.sh reconnect
```

### Відключитися від WiFi

```bash
sudo /path/to/cleanup_scripts/mikrotik_wifi_spoof.sh disconnect
```

## Конфігурація

Конфігурація можна редагувати в скрипті:

```bash
MIKROTIK_HOST="192.168.88.1"      # IP адреса мікротік
MIKROTIK_USER="admin"              # Користувач мікротік
SSH_KEY="~/.ssh/id_ed25519"        # Шлях до SSH ключа
GUEST_WIFI_INTERFACES=("wifi3" "wifi4")  # Гостьові WiFi інтерфейси
GUEST_WIFI_PASSWORD="00000000"     # Пароль гостьової мережи
```

## Отримані SSID nombres

SSID формуються у форматі: `Guest_XXXXXX`

Де `XXXXXX` - 6 випадкових символів (букви + цифри).

Приклади:
- `Guest_AB12CD`
- `Guest_ZX9K2L`
- `Guest_RENHWDPE`

## IP підмережа

IP підмережа формується як: `10.x.y.0/24`

Де `x` та `y` - випадкові числа від 1 до 254.

Приклади:
- `10.143.165.0/24` (DHCP: 143.165.100-200)
- `10.16.127.0/24` (DHCP: 16.127.100-200)

## MAC адреса

MAC адреси мають формат: `02:XX:XX:XX:XX:XX`

Де `02` означає "локально керована, унікастна" адреса (запобігає глобальному отриманню).

Приклад: `02:75:9E:66:FA:1D`

## Інтеграція з системою очистки редакторів

Модуль доступний у всіх редакторів:
- Windsurf
- VS Code / VS Code OSS
- Antigravity Editor
- Cursor IDE
- Continue CLI

Для запуску через систему очистки:

```bash
# Через cleanup_modules.json
python3 /path/to/main.py cleanup --module mikrotik_wifi_spoof
```

## Розширена Python версія

Також доступна Python версія для більш гнучкого управління:

```bash
python3 providers/mikrotik_wifi_spoofing.py --spoof --verbose
python3 providers/mikrotik_wifi_spoofing.py --status --json
python3 providers/mikrotik_wifi_spoofing.py --verify
```

## Логування

Логи зберігаються в:
- `/tmp/mikrotik_wifi_spoof_*.log` - shell скрипт логи
- `/tmp/mikrotik_wifi_spoof.log` - Python модуль логи

## Проблеми та рішення

### "Cannot connect to MikroTik"

- Перевірте SSH ключ налаштований на MikroTik
- Перевірте IP адресу мікротік (192.168.88.1)
- Перевірте мережеву доступність: `ping 192.168.88.1`

### "Failed to change MAC address"

- Вимагається `sudo` привілеї
- Может бути не всі інтерфейси підтримують зміну MAC (віртуальні інтерфейси)
- На M1/M2 Mac може потребуватися інший підхід

### WiFi не змінилася

- Перевірте назви інтерфейсів на мікротік: `/interface wifi print`
- Переконайтесь, що інтерфейси включені та працюють

## Безпека

⚠️ **Важливо:**
- Пароль гостьової мережи лишається `00000000` (легко вгадується)
- Ця система захищає від трекінгу за IP/MAC, але не від інших методів
- Регулярно запускайте модуль для частої зміни параметрів
- Переконайтесь, що SSH доступ до мікротік захищений

## Автоматизація

Для автоматичного запуску при кожному включенні редактора, додайте до вашої системи старту:

```bash
# Додати до ~/.zshrc або ~/.bash_profile для легкого доступу
alias wifi-spoof="sudo /path/to/cleanup_scripts/mikrotik_wifi_spoof.sh spoof"
alias wifi-spoof-auto="sudo /path/to/cleanup_scripts/mikrotik_wifi_spoof.sh spoof-auto"
alias wifi-status="/path/to/cleanup_scripts/mikrotik_wifi_spoof.sh status"
alias wifi-disconnect="sudo /path/to/cleanup_scripts/mikrotik_wifi_spoof.sh disconnect"
```

Потім просто запустіть:
```bash
wifi-spoof-auto  # Повна анонімність за один раз
```

Або налаштуйте cron для періодичного запуску:

```bash
# Кожні 2 години з автоперепідключенням
0 */2 * * * /usr/bin/sudo /path/to/cleanup_scripts/mikrotik_wifi_spoof.sh spoof-auto >> /var/log/wifi_spoof.log 2>&1

# Кожні 4 години без автоперепідключення (якщо з'єднання непостійне)
0 */4 * * * /usr/bin/sudo /path/to/cleanup_scripts/mikrotik_wifi_spoof.sh spoof >> /var/log/wifi_spoof.log 2>&1
```

## Приклади використання

### Приклад 1: Одноразовий спойфинг з автоперепідключенням

```bash
$ sudo ./cleanup_scripts/mikrotik_wifi_spoof.sh spoof-auto

╔════════════════════════════════════════════════════════════╗
║  MikroTik WiFi & MAC Address Spoofing Cleanup Module       ║
╚════════════════════════════════════════════════════════════╝

ℹ Checking MikroTik connection...
✓ MikroTik is accessible
ℹ Generated new configuration:
  WiFi SSID:    Guest_P7DR+ATY
  IP Subnet:    10.166.45.0/24
  MAC Address:  02:F2:C4:5B:0D:2E

✓ WiFi SSID updated on all guest interfaces
✓ IP pool updated

✓ Spoofing completed!
✓ Auto-reconnect completed!
✓ Successfully connected to: Guest_P7DR+ATY
```

### Приклад 2: Перепідключення до певної мережи

```bash
$ ./cleanup_scripts/mikrotik_wifi_spoof.sh reconnect "Guest_ABC123"

ℹ Connecting to: Guest_ABC123 with password: 00000000
✓ Connection command sent to: Guest_ABC123
✓ Successfully connected to: Guest_ABC123
```

### Приклад 3: Перевірка статусу

```bash
$ ./cleanup_scripts/mikrotik_wifi_spoof.sh status

ℹ Retrieving MikroTik WiFi status...
#     NAME   MASTER-INTERFACE  CONFIGURATION.MODE  CONFIGURATION.SSID
0 MBR wifi1                    ap                  Solo5             
1 MBR wifi2                    ap                  Solo              
2  B  wifi3  wifi1                                 Guest_P7DR+ATY    
3  B  wifi4  wifi2                                 Guest_P7DR+ATY    

ℹ Local MAC addresses:
        ether 02:F2:C4:5B:0D:2E
        ether 6a:a8:5c:c0:b5:65
```

## Як працює

```
1. Подія: Запуск скрипту або модуля
   ↓
2. Генерація: Випадкова SSID, IP підмережа, MAC адреса
   ↓
3. Оновлення MikroTik:
   - SSH підключення до мікротік
   - Оновлення SSID для wifi3 та wifi4 інтерфейсів
   - Оновлення IP pool для DHCP
   ↓
4. Оновлення macOS:
   - Детекція активного мережевого інтерфейсу
   - Зміна локальної MAC адреси
   ↓
5. (Опційно) Автоперепідключення:
   - Відключення від старої мережи
   - Включення WiFi
   - Підключення до нової SSID
   - Перевірка підключення
   ↓
6. Результат: Нова мережа з новими параметрами
```

## Режим Spoof-Auto

Режим `spoof-auto` - це рекомендований спосіб для комплексної зміни мережевої ідентичності:

```bash
sudo ./cleanup_scripts/mikrotik_wifi_spoof.sh spoof-auto
```

### Етапи виконання:

1. **Генерація** (< 1 сек)
   - Нова SSID: `Guest_XXXXXX`
   - Новий сабнет: `10.x.y.0/24`
   - Нова MAC: `02:XX:XX:XX:XX:XX`

2. **Оновлення MikroTik** (2-5 сек)
   - SSH підключення
   - Оновлення двох WiFi інтерфейсів
   - Оновлення DHCP池

3. **Змінення MAC** (1-2 сек)
   - Локальна зміна MAC адреси

4. **Автоперепідключення** (10-30 сек)
   - Вимкнення старої мережи
   - Включення WiFi
   - Підключення до нової SSID
   - Отримання IP з нового діапазону

**Загальний час**: ~30-45 секунд на повне оновлення

### Переваги спойфинга з автоперепідключенням:

✅ Автоматична зміна всіх параметрів  
✅ Нема потребі вручну вибирати мережу  
✅ Одна команда для повної анонімності  
✅ Нова IP адреса гарантована  
✅ Мінімальний час без інтернету

## Автор

Розроблено для проекту System Privacy Protection.

## Ліцензія

GNU GPL v3
