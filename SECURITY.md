# 🔐 Поради щодо безпеки Windsurf Cleanup System

## ⚠️ Важливі застереження

### 1. Бекапи у /tmp
- **Проблема:** Директорія `/tmp` очищається при перезавантаженні
- **Рішення:** Копіюйте бекапи в безпечне місце

```bash
# Зберегти бекап назавжди
BACKUP_DIR=$(ls -td /tmp/windsurf_backup_* 2>/dev/null | head -1)
cp -r "$BACKUP_DIR" ~/Documents/windsurf_backup_permanent_$(date +%Y%m%d_%H%M%S)
```

### 2. Sudo права
- **Що потребує sudo:**
  - Зміна hostname (`scutil`)
  - Очищення DNS кешу
  - Видалення системних логів
  
- **Рекомендація:** Перевіряйте скрипт перед запуском з sudo

### 3. Keychain
- **Що видаляється:**
  - Всі токени автентифікації Windsurf
  - OAuth credentials
  - Збережені паролі
  
- **Важливо:** Після відновлення можливо доведеться повторно авторизуватися

### 4. Процес у фоні
- **PID зберігається тільки в сесії терміналу**
- **Рекомендація:** Запишіть PID або використовуйте:

```bash
# Знайти процес автовідновлення
ps aux | grep "sleep 18000" | grep -v grep
```

## 🛡️ Що МОЖНА робити безпечно

✅ Запускати Windsurf (як новий клієнт)  
✅ Працювати з Mac  
✅ Закривати термінал (процес у фоні продовжить працювати)  
✅ Копіювати бекапи  
✅ Перевіряти статус  

## ❌ Що НЕ МОЖНА робити

❌ Перезавантажувати Mac (якщо потрібне автовідновлення)  
❌ Вимикати Mac  
❌ Видаляти `/tmp/windsurf_backup_*` вручну  
❌ Вбивати процес автовідновлення  
❌ Запускати cleanup повторно (створить новий бекап і процес)  

## 🔄 Відновлення після збоїв

### Сценарій 1: Перезавантажили Mac передчасно

```bash
# Бекапи видалено, автовідновлення неможливе
# Windsurf назавжди залишиться "новим клієнтом"
# Якщо потрібен старий обліковий запис - переустановіть Windsurf
```

### Сценарій 2: Вбили процес відновлення

```bash
# 1. Перевірте чи є бекап
ls -ld /tmp/windsurf_backup_*

# 2. Якщо є - відновіть вручну
./restore_windsurf_backup.sh
```

### Сценарій 3: Видалили бекап випадково

```bash
# Якщо не копіювали - відновлення неможливе
# Windsurf залишиться з новими ідентифікаторами
# Це не критично, просто будете новим клієнтом
```

## 💾 Стратегія бекапів

### Короткострокове використання (до 5 годин)
```bash
# Просто запустіть cleanup і чекайте
./deep_windsurf_cleanup.sh
# Автовідновлення спрацює автоматично
```

### Довгострокове використання (більше 5 годин)
```bash
# 1. Запустіть cleanup
./deep_windsurf_cleanup.sh

# 2. ОДРАЗУ скопіюйте бекап
sleep 10  # Дочекайтеся створення бекапу
BACKUP_DIR=$(ls -td /tmp/windsurf_backup_* 2>/dev/null | head -1)
cp -r "$BACKUP_DIR" ~/Documents/windsurf_permanent_backup

# 3. Працюйте скільки потрібно

# 4. Коли захочете повернутися - відновіть вручну
# Скопіюйте файли з ~/Documents/windsurf_permanent_backup
# до відповідних директорій Windsurf
```

## 🔍 Перевірка цілісності

### Перед запуском cleanup
```bash
# Збережіть поточні ідентифікатори для порівняння
cat ~/Library/Application\ Support/Windsurf/machineid > /tmp/old_machineid.txt
cat ~/Library/Application\ Support/Windsurf/storage.json > /tmp/old_storage.json
```

### Після cleanup
```bash
# Переконайтеся що ідентифікатори змінилися
diff /tmp/old_machineid.txt ~/Library/Application\ Support/Windsurf/machineid
# Має показати різницю
```

### Після відновлення
```bash
# Переконайтеся що ідентифікатори повернулися
diff /tmp/old_machineid.txt ~/Library/Application\ Support/Windsurf/machineid
# Не має показувати різницю
```

## 🚨 Екстрена допомога

### Windsurf не запускається після cleanup
```bash
# 1. Відновіть оригінальні ідентифікатори
./restore_windsurf_backup.sh

# 2. Якщо не допомогло - повне перевстановлення
rm -rf ~/Library/Application\ Support/Windsurf
# Завантажте і встановіть Windsurf знову
```

### Втратили всі дані
```bash
# Якщо не створили permanent backup:
# 1. Прийміть що це новий клієнт
# 2. Створіть новий обліковий запис Windsurf
# 3. Переналаштуйте extensions та settings
```

### Hostname не повертається
```bash
# Вручну поверніть hostname
sudo scutil --set HostName "DEVs-Mac-Studio"  # Або ваш оригінальний
sudo scutil --set LocalHostName "DEVs-Mac-Studio"
sudo scutil --set ComputerName "DEVs-Mac-Studio"
sudo dscacheutil -flushcache
sudo killall -HUP mDNSResponder
```

## 📊 Моніторинг

### Створіть cronjob для перевірки статусу
```bash
# Кожні 30 хвилин перевіряти процес
# НЕ рекомендується для production, тільки для моніторингу

# Відкрийте crontab
crontab -e

# Додайте:
*/30 * * * * ps aux | grep "sleep 18000" | grep -v grep || echo "Процес відновлення не активний!" >> ~/windsurf_cleanup_monitor.log
```

## 🎓 Висновки

1. **Завжди копіюйте бекапи** якщо плануєте перезавантаження
2. **Записуйте PID процесу** для контролю
3. **Не паніліуйте** якщо щось пішло не так - це не критично
4. **Читайте лог файли** Windsurf для діагностики: `~/Library/Logs/Windsurf/`
5. **Тестуйте на тестовому Mac** перед production використанням

---

**Пам'ятайте:** Цей скрипт створений для тимчасової зміни ідентифікації. Він НЕ призначений для обходу ліцензування або інших обмежень. Використовуйте відповідально!
